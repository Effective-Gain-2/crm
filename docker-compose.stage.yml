services:
  backend:
    build: ./
    restart: always
    expose:
      - "3002"
      - "3333"

  frontend:
    build:
      context: ./client/whatsapp-web-js-client
      args:
        REACT_APP_URL: https://backend-stage.effectivegain.com
    restart: always
    expose:
      - "80"
